org: essexpads
service: live-track

provider:
  name: aws
  runtime: nodejs20.x
  iam:
    role:
      statements:
        - Effect: Allow
          Action: "secretsmanager:GetSecretValue"
          Resource: "*"

functions:
  inbound:
    handler: inbound.exec
    url: true
